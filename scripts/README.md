# Scripts

- `sanity_check_j_thresholds.py`: finite off-by-one sanity check for the packet's `J`, `R`, and `A` definitions. It is not a proof.
- `build_release.py`: verifies expected files, runs the sanity checker, writes `MANIFEST.md`, and creates a release zip.
